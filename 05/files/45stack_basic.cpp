#include <iostream>

using namespace std;

/**
 * tack 基本概念
 * 概念：
 *  stack是一种先进后出的数据结构，它只有一个出口
 *  栈中只有顶端的元素才可以被外界使用，因此栈不允许有遍历行为
 *  栈中进入数据称为  --- 入栈  push
 *  栈中弹出数据称为  --- 出栈  pop
 *
 * 栈不允许遍历,只有栈顶元素可以被访问
 *
 * 有 empty() 函数判断是否为空 为空返回true,不为空返回false
 * 有 size() 函数返回个数
 */

int main() {

    return 0;
}
